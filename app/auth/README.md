# Auth Directory

This directory contains authentication-related route handlers under `/auth`.

## Included Routes

- `/auth/signout` (POST): Signs out the current Supabase session on the server and redirects to `/`.

## Notes

This folder currently contains route handlers rather than page components.
